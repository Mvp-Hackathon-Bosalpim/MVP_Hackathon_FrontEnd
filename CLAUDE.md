# 프로젝트 컨벤션 (Comfozi)

이 문서는 Claude Code가 이 저장소에서 작업할 때 항상 참고해야 하는 규칙입니다.

## 프로젝트 개요
- 2주 해커톤, MVP 개발/배포 목표
- **Comfozi**: 구매 데이터 검수 시스템 (검수 인박스) — 업로드 → 구조화 → 예외 탐지 → 사람 검토 → 내보내기 파이프라인
- 프론트엔드는 담당자 2인, 백엔드는 Java/Spring 팀
- 저장소: Mvp-Hackathon-Bosalpim/MVP_Hackathon_FrontEnd

## 기술 스택
- React (.jsx), axios, TailwindCSS
- 전역 상태: Context API / 서버 상태: TanStack Query
- 파일 네이밍: 케밥케이스 (예: dashboard-page.jsx, stat-card.jsx) — 기존 파스칼케이스 파일도 전체 케밥케이스로 통일 완료
- 반응형: 처음부터 단일 코드베이스로 반응형 구축 (단, register-page.jsx는 현재 미반응형/고정 px, 반응형은 "시간 남으면" 진행하기로 팀 합의)

## 폴더 구조 (layered structure)
```
src/
├── components/
│   ├── layout/   (헤더 등 공통 레이아웃)
│   └── ui/        (재사용 UI 컴포넌트)
├── pages/         (라우트 단위 화면)
├── mocks/         (API 준비 전 임시 데이터)
```
- 공통 레이아웃(헤더 등)은 MainLayout + `<Outlet />` 구조로 관리, 새 페이지는 이 하위 라우트로 등록

## Git 컨벤션
- 기준 브랜치: `main` (develop 없음)
- 브랜치명: `feat/#이슈번호` (# 기호 포함, 설명 생략)
  - 생성 시 따옴표로 감싸기: `git checkout -b "feat/#4"`
- 이슈 제목: `[타입] 설명` (예: `[Feat] 헤더 & 대시보드 레이아웃 구현`)
- 커밋 메시지: `분류 : 설명` (대괄호 없이 콜론 방식, 이슈번호 생략)
  - 예: `feat : 등록 페이지 컴포넌트 분리, 탭 UI 및 파일 업로드 기능 구현`
  - 커밋 타입: `feat`(기능추가) `fix`(버그수정) `docs`(문서수정) `build`(빌드파일수정) `style`(코드포맷팅) `refactor`(리팩토링) `test`(테스트코드) `merge`(병합, 예: `merge : #3 헤더 및 대시보드 레이아웃 병합`) `design`(CSS/UI디자인변경) `comment`(주석) `rename`(파일/변수/폴더명변경) `remove`(파일/폴더삭제)
- PR 대상: `main`
- PR 설명에 `closes #이슈번호` 필수 포함
- 빌드 체크 + 리뷰 승인 후 머지

## 색상/디자인 토큰 (index.css 기준)
- **하드코딩 절대 금지**, 반드시 정의된 토큰 클래스 사용
- Comfozi Navy: `primary-navy` (#1A3356) — 메인 버튼, 헤더
- Gold: `primary-gold` (#C89C4A) — 강조, 중요 상태
- Surface: `surface-0` / `surface-100` / `surface-200` / `surface-300`(#D8D2C5) — 배경 단계
- Gray: `gray-100`(#AEB7C2) / `gray-200`(#DEE1E5) / `gray-300`(#667085) / `gray-500` / `gray-700` — 텍스트 위계
  - ⚠️ 주의: `gray-100`이 `gray-300`보다 오히려 더 진한 톤입니다 (숫자와 실제 명도가 반대)
- 새 토큰이 필요하면 임의로 추가하지 말고 먼저 팀에 확인

### 미확정 — 상태 색상 토큰 (state-error / state-success / state-warning)
등록 페이지(file-upload-section.jsx, manual-entry-section.jsx)에 아래 색상이 토큰 없이 하드코딩되어 있음:
- `#AD211D` (에러/실패)
- `#74996B`, `#1E9800` (성공 — 두 값 혼재, 통일 필요)
- `#F07800` (경고)
- `#D5A548` (골드 계열, 기존 primary-gold와 다른 값)

→ 토큰 신설을 팀에 제안 예정, 아직 index.css에 미반영. **팀 확인 전까지 신규 코드에서 이 값들 그대로 사용 금지.**

(참고: 대시보드 우선 검수 목록의 누락/중복/불일치 색상은 별도의 기존 하드코딩이며 이번 정리 대상 아님 — 건드리지 않음)

## 코드 작성 원칙
- 데이터(숫자 등) 하드코딩 금지 → 실제 API 없을 때는 `value ?? "-"` 형태로 빈 값 처리
- 반복되는 UI는 `components/ui`에 재사용 컴포넌트로 분리, 데이터는 props로 전달
- 아이콘 등도 컴포넌트를 props로 전달해 재사용 (예: `<StatCard icon={FileText} .../>`)

## 자주 쓰는 레이아웃 패턴
- 나란히 배치된 카드 높이를 맞출 때: 각 카드에 `h-full` + `flex flex-col`
- 카드 내부 콘텐츠 균등 배분: `flex-1` + `justify-evenly`
- 요소 간 위치를 화면폭과 무관하게 고정: `gap`보다 `ml-`(margin-left) 방식 사용
- 여러 요소를 화면 크기와 무관하게 비율로 배치: 고정폭(`w-`, `ml-`) 대신 `flex-[숫자]` 비율 방식
  - flex 아이템 내부 콘텐츠가 왼쪽 정렬돼서 비율만으로 안 밀리면 `flex-[비율]`에 `ml-` 값 병행
  - 텍스트 길이가 다른 항목들의 시작 위치를 맞추려면 해당 텍스트에 고정 `w-` 값 부여 후 `truncate` 처리

## 역할 분담 (⚠️ 반드시 지킬 것)
- `register-page.jsx`: 원래 정민님 파일(마크업)이었으나, 정민님 확인 하에 기능 구현(컴포넌트 분리, 파일 업로드/수기 등록 기능)을 이어받아 작업 중
  - 이슈 #2(정민님, 마크업)와 이슈 #5(본인, 기능 구현)로 분리됨
- **정민님이 만든 파일은 직접 수정하지 않고, 필요한 변경은 정민님께 요청**
- 페이지 간 데이터 전달은 `navigate(경로, { state: {...} })`로 처리
- 받는 쪽(`useLocation`)의 파일이 본인 소유가 아니면 **코드 제안만 하고 직접 수정하지 않음**

## 스코프 원칙
- 가격 판단 자동화, 자동 승인, 결과값 하드코딩은 범위 밖 — 사람 검토를 대체하는 로직 만들지 말 것

## 작업 스타일
- 사용자(덕림님)는 '바이브 코더' — 코드를 AI로 생성하지만 이해하고 넘어가고 싶어함
- 코드 작업 전에 "무엇을, 왜 만드는지" 먼저 설명하고 합의된 후 코드 작성
- 여러 요소가 있는 화면(대시보드 등)은 한 번에 다 만들지 않고, 섹션/카드 단위로 하나씩 만들고 확인받은 후 다음으로 진행
- 설명은 쉬운 말로